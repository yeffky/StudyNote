# Vue Router

* VueRouter是Vue的官方路由
* Vue的单页面应用是基于路由和组件的，路由用于设定访问路径，将路径和组件映射
* 3.x版本和vue2对应(npm install vue-router@3)

## 一、组成

### 1. VueRouter

路由器类，根据路由请求在路由视图中动态渲染选中的组件，其中包括路由表，即页面。

### 2. \<router-link>

请求链接组件，浏览器会解析成\<a>（路由请求）

### 3. \<router-view>

动态视图组件，用来渲染展示与路由路径对应的组件（路由展示组件更新）
